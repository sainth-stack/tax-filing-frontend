name: CICD

on:
  push:
    branches:
      - development

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        run: echo "${{ secrets.PASSWORD }}" | docker login -u "${{ secrets.USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: docker build -t sainath521/reactjs-app .

      - name: Publish image to Docker Hub
        run: docker push sainath521/reactjs-app:latest   

  deploy: 
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH client
        run: sudo apt-get install -y openssh-client

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.AWS_SECRET_KEY }}

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@13.229.92.87 << 'EOF'
            sudo bash -c "
              # Pull the latest Docker image
              docker pull sainath521/reactjs-app:latest

              # Stop and remove the old Docker container if it exists
              docker rm -f reactContainer || true

              # Run the new Docker container
              docker run -d -p 4000:4000 --name reactContainer sainath521/reactjs-app:latest
            "
          EOF